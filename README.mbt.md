# MoonLexKit

MoonLexKit is a MoonBit lexical scanning and lightweight parsing toolkit.

It is a separate contest project from MoonNavKit and MoonSketchKit. MoonNavKit
focuses on path planning; MoonSketchKit focuses on probabilistic data
structures; MoonLexKit focuses on tokenizer, token stream, diagnostics, and
small parser helpers.

